<!-- 写作边界：本文档是需求文档，只写"做什么"和"为什么"。
     允许写入：用户场景、业务规则、验收标准、ASCII 原型图（用户视角的页面布局和交互流程）。
     禁止写入：代码片段、API 设计、数据库 schema、技术方案、组件结构、实现步骤。
     技术设计由下游 feat-review-design skill 负责。 -->

# Feat-{iid}: {功能简述}

> 更新日期：{date}
> 来源：{source}
> 优先级：{priority}

## 1. 功能概述

### 1.1 背景
{背景描述}

### 1.2 目标
{目标描述}

### 1.3 非目标
- {非目标1}
- {非目标2}

## 2. 业务实体

涉及的核心业务概念（只描述概念和关系，不定义表结构或字段）：

| 实体 | 说明 | 关键属性 | 关系 |
|------|------|----------|------|
| {实体1} | {概念说明} | {关键业务属性} | 关联 {实体2}（1:N） |
| {实体2} | {概念说明} | {关键业务属性} | |

## 3. 业务规则

跨场景的校验规则和约束：

| 编号 | 规则 | 触发条件 | 违规处理 |
|------|------|----------|----------|
| BR-001 | {规则描述} | {何时校验} | {如何反馈} |
| BR-002 | | | |

## 4. 原型

### 4.1 页面结构

```
┌─────────────────────────────────────────┐
│  {页面名称}                              │
├─────────────────────────────────────────┤
│  ┌─────────┐  ┌─────────┐               │
│  │ 区域 1  │  │ 区域 2  │  ...          │
│  └─────────┘  └─────────┘               │
├─────────────────────────────────────────┤
│  {关键交互说明}                           │
└─────────────────────────────────────────┘
```

### 4.2 交互流程

```
用户操作: {触发动作}
  │
  ▼
{页面A} ── {动作1} ──▶ {页面B}
  │                       │
  └── {动作2} ──▶ {页面C}
```

> 复杂交互可补充状态流转图：`{状态A} ──{事件}──▶ {状态B}`

## 5. 用户场景

### 场景 1: {场景名称1}
- **角色**：{角色}
- **前置条件**：{进入场景前必须满足的条件}
- **触发**：{触发条件}
- **正常流程**：
  1. {用户视角的操作步骤 1}
  2. {操作步骤 2}
  3. {操作步骤 3}
- **结果**：{期望结果}
- **异常流程**：
  - {异常条件 1} → {用户看到的反馈/错误提示}
  - {异常条件 2} → {用户看到的反馈/错误提示}

### 场景 2: {场景名称2}
- **角色**：
- **前置条件**：
- **触发**：
- **正常流程**：
- **结果**：
- **异常流程**：

## 6. 依赖关系

| 依赖项 | 类型 | 说明 | 状态 |
|--------|------|------|------|
| {模块/服务名} | 上游依赖 | {需要它提供什么} | 已有 / 待开发 |
| {外部系统} | 外部接口 | {调用什么API} | 已有 / 需对接 |
| {数据来源} | 数据依赖 | {需要什么数据} | 已有 / 需新建 |

## 7. 验收标准

### 功能验收
- [ ] [场景1] {可验证的功能条件 1}
- [ ] [场景1] {可验证的功能条件 2}
- [ ] [场景2] {可验证的功能条件 3}

### 非功能验收
- [ ] 性能：{响应时间/吞吐量要求}
- [ ] 安全：{权限/数据保护要求}
- [ ] 兼容：{浏览器/版本/数据迁移要求}
- [ ] 可用性：{错误提示/空态/加载态要求}

## 8. 约束条件

- {技术约束}
- {业务约束}
- {依赖约束}
