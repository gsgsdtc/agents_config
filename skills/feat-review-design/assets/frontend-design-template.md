# Frontend Design: {feat 编号}-{功能简述}

> 所属模块：{模块名}
> 关联需求：docs/feat/feat-{iid}-{slug}.md
> 关联后端设计：{docs/modules/<module>/design/<num>-<slug>-backend-design.md 或 无}
> 目标平台：Web / iOS / Android
> 更新日期：{date}
> 状态：草稿 / 已确认 / 已实现

## 1. 设计概述

### 1.1 目标
{用一两句话说明这个前端设计要解决什么问题}

### 1.2 设计约束
- 响应式：{支持的断点/设备}
- 无障碍：{WCAG 级别要求}
- 性能指标：{LCP / FID / CLS 目标值}
- 浏览器兼容：{最低版本要求}
- {来自 feat 的其他约束}
- {来自 module spec 的现有前端架构约束}

## 2. 页面原型

### 2.1 页面清单

| 页面名称 | 路由 | 类型 | 优先级 | 说明 |
|----------|------|------|--------|------|
| {页面1} | /path | SSR/CSR/Static | P0 | {说明} |
| {页面2} | /path | SSR/CSR/Static | P1 | {说明} |

### 2.2 页面线框图

**{页面名称}**

```
┌─────────────────────────────────────────┐
│  Header / Navigation                    │
├─────────────────────────────────────────┤
│                                         │
│  {主要内容区域的 ASCII Art 布局}         │
│                                         │
│  ┌──────────┐  ┌──────────────────────┐ │
│  │ Sidebar  │  │ Main Content         │ │
│  │          │  │                      │ │
│  │          │  │                      │ │
│  └──────────┘  └──────────────────────┘ │
│                                         │
├─────────────────────────────────────────┤
│  Footer                                 │
└─────────────────────────────────────────┘
```

{为每个页面提供线框图}

### 2.3 页面交互关系

**页面流转图**

```
{用 ASCII 表示所有页面之间的跳转关系和触发条件}

[页面A] ──{用户操作/事件}──▶ [页面B] ──{操作/事件}──▶ [页面C]
    ▲                            │
    └──────{返回/取消}───────────┘
```

**页面间数据传递**

| 来源页面 | 目标页面 | 触发动作 | 传递数据 | 传递方式 |
|----------|----------|----------|----------|----------|
| {页面A} | {页面B} | {点击/提交/回调} | {传递的参数或状态} | URL Params / State / Storage |

**页面入口与出口条件**

| 页面 | 入口条件（何时可进入） | 出口条件（何时离开） | 异常处理 |
|------|----------------------|---------------------|----------|
| {页面A} | {前置条件，如：已登录、有权限} | {正常流程：提交成功} | {异常：超时→提示重试} |
| {页面B} | {前置条件，如：来自页面A的跳转} | {正常流程：操作完成} | {异常：无权限→403} |

## 3. 组件层次结构

### 3.1 组件树

```
App
├── Layout
│   ├── Header
│   │   ├── Logo
│   │   └── Navigation
│   ├── Sidebar (if applicable)
│   └── Footer
├── {PageName}Page
│   ├── {SectionComponent}
│   │   ├── {ChildComponent}
│   │   └── {ChildComponent}
│   └── {SectionComponent}
└── ...
```

### 3.2 共享组件

| 组件名 | 职责 | Props | 复用页面 |
|--------|------|-------|----------|
| {ComponentName} | {职责描述} | {关键 props} | {使用该组件的页面} |

### 3.3 页面专属组件

| 所属页面 | 组件名 | 职责 | 状态需求 |
|----------|--------|------|----------|
| {页面} | {ComponentName} | {职责描述} | {本地/全局/无} |

## 4. 状态管理设计

### 4.1 状态分层

| 状态类型 | 数据 | 管理方式 | 说明 |
|----------|------|----------|------|
| 服务端状态 | {API 返回的数据} | SWR / React Query / Server State | {缓存策略} |
| URL 状态 | {搜索/筛选/分页参数} | URL Search Params | {同步方式} |
| 表单状态 | {表单字段} | React Hook Form / useState | {验证时机} |
| UI 状态 | {模态框/折叠/选中等} | useState / useReducer | {作用范围} |
| 全局状态 | {用户信息/主题/权限等} | Context / Zustand / Redux | {必要性说明} |

### 4.2 状态流转图

```
{关键状态的流转图，使用 ASCII 表示}

User Action → State Change → UI Update
     │              │              │
     ▼              ▼              ▼
  {action}    {state mutation}  {re-render}
```

## 5. 数据获取策略

### 5.1 API 依赖

| 页面/组件 | API 端点 | 方法 | 触发时机 | 说明 |
|-----------|----------|------|----------|------|
| {页面/组件} | /api/{endpoint} | GET/POST | 页面加载/用户操作 | {说明} |

### 5.2 SSR/CSR 决策

| 数据 | 获取方式 | 原因 |
|------|----------|------|
| {数据1} | SSR (getServerSideProps / Server Component) | {SEO/首屏/权限} |
| {数据2} | CSR (useEffect / SWR) | {交互触发/实时更新} |
| {数据3} | Static (getStaticProps / ISR) | {不常变/可缓存} |

### 5.3 加载/空/错误状态

| 页面/组件 | 加载态 | 空数据态 | 错误态 |
|-----------|--------|----------|--------|
| {页面/组件} | {Skeleton/Spinner/描述} | {空态 UI 描述} | {错误 UI + 重试策略} |

## 6. 路由设计

### 6.1 路由表

| 路径 | 页面组件 | 布局 | 权限 | 说明 |
|------|----------|------|------|------|
| /path | {PageComponent} | {LayoutName} | public/auth/role | {说明} |
| /path/:id | {PageComponent} | {LayoutName} | public/auth/role | {说明} |

### 6.2 导航流程图

```
{页面间导航关系，使用 ASCII 表示}

[Page A] ──click──▶ [Page B] ──submit──▶ [Page C]
                         │
                    ──cancel──▶ [Page A]
```

### 6.3 受保护路由

| 路由模式 | 保护类型 | 未授权行为 |
|----------|----------|------------|
| /admin/* | 角色：admin | 重定向到 /403 |
| /dashboard/* | 认证：登录 | 重定向到 /login |

## 7. 交互流程

### 7.1 关键交互序列

**{交互名称}**（如：表单提交流程）

```
用户操作          前端处理            API 调用           UI 反馈
─────────────────────────────────────────────────────────────
填写表单    →  实时校验字段    →                    →  显示校验结果
点击提交    →  整体校验        →                    →  禁用按钮+Loading
            →                  →  POST /api/xxx     →
            →  处理响应        →                    →  成功提示/错误展示
            →  状态更新        →                    →  页面跳转/刷新列表
```

### 7.2 表单校验规则

| 字段 | 规则 | 校验时机 | 错误提示 |
|------|------|----------|----------|
| {fieldName} | required / min / max / pattern | onChange/onBlur/onSubmit | {提示文案} |

## 8. 响应式设计

### 8.1 断点策略

| 断点名称 | 范围 | 布局变化 |
|----------|------|----------|
| mobile | < 640px | {布局描述} |
| tablet | 640px - 1024px | {布局描述} |
| desktop | > 1024px | {布局描述} |

### 8.2 关键适配说明

- {组件/页面}：{在不同断点下的具体适配方案}
- {组件/页面}：{在不同断点下的具体适配方案}

## 9. 测试方案

### 9.1 测试策略

| 层级 | 范围 | 工具 | 覆盖重点 |
|------|------|------|----------|
| 组件测试 | {范围} | Vitest + Testing Library | 渲染/交互/状态 |
| E2E 测试 | {范围} | Playwright | 关键用户流程 |
| 视觉测试 | {范围} | Chromatic / Percy | 样式回归 |

### 9.2 E2E 测试用例

> 由 design-review-dev 步骤 12 通过 agent-browser 自动执行。每个用例对应一条验收标准。

| ID | 对应验收标准 | 起始页面 | 操作步骤 | 期望结果 | 截图点 |
|----|-------------|----------|----------|----------|--------|
| E2E-001 | {验收标准描述} | /path | 1. {步骤1} 2. {步骤2} | {页面显示/跳转/数据} | {关键截图位置} |
| E2E-002 | {验收标准描述} | /path | 1. {步骤1} 2. {步骤2} | {页面显示/跳转/数据} | {关键截图位置} |
| E2E-003 | {异常场景} | /path | 1. {步骤1} 2. {步骤2} | {错误提示/空态} | {关键截图位置} |

### 9.3 关键测试用例

| 用例 | 类型 | 验证点 |
|------|------|--------|
| {用例1} | 组件/E2E | {验证什么行为} |

## 10. 影响评估

### 10.1 对现有前端的影响
- {影响1}
- {影响2}

### 10.2 后端接口依赖

| API 端点 | 状态 | 说明 |
|----------|------|------|
| /api/{endpoint} | 已有/待开发 | {前端使用方式} |

### 10.3 代码生成指引

> 提示：使用 `/ui-gen` 直接从本设计文档生成完整前端代码。
> ui-gen 将读取本文档的 §2-§8 章节，先生成共享基础设施（路由、布局、共享组件、状态管理、API 层），再逐页面生成代码。
